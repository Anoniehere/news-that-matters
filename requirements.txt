# Direct dependencies only — pip resolves transitive versions.
# Pinning transitive deps causes version conflicts across platforms.

# Web framework
fastapi>=0.110.0
uvicorn>=0.27.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# AI / LLM
google-generativeai>=0.8.0
google-genai>=1.0.0

# RSS + HTML parsing
feedparser>=6.0.0
lxml>=5.0.0
requests>=2.31.0

# ML — clustering + TF-IDF (no torch, no sentence-transformers)
scikit-learn>=1.4.0
scipy>=1.12.0
numpy>=1.26.0

# Background scheduler
apscheduler>=3.10.0

# Env + utils
python-dotenv>=1.0.0
httpx>=0.26.0
